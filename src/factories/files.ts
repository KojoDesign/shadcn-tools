import { defineFile } from "./helpers.ts";

export const component = defineFile("registry:component");
export const hook = defineFile("registry:hook");
export const lib = defineFile("registry:lib");
export const ui = defineFile("registry:ui");
export const block = defineFile("registry:block");
